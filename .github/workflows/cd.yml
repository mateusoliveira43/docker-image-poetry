name: Continuos Delivery

on:
  push:
    branches:
      - main

jobs:
  generate-jobs:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.generate-jobs.outputs.matrix }}
    steps:
      - uses: actions/checkout@v3

      - name: Generate jobs
        id: generate-jobs
        run: |
          matrix=$(./generate_cd_jobs.py)
          echo "::set-output name=matrix::$matrix"

  docker-hub:
    needs: generate-jobs
    strategy:
      matrix: ${{ fromJson(needs.generate-jobs.outputs.matrix) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: mateusoliveira43
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: |
          echo "${{ matrix.dockerfile }}" > Dockerfile
          docker build --tag mateusoliveira43/poetry:${{ matrix.version }} - < Dockerfile

      - name: Push Docker image
        run: docker push mateusoliveira43/poetry:${{ matrix.version }}

